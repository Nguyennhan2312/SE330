# Ghi đè cấu hình khi chạy local (mvn spring-boot:run -Dspring.profiles.active=local)
# Thay đổi thông tin kết nối PostgreSQL nếu khác mặc định:
spring.datasource.url=jdbc:postgresql://localhost:5432/library_db
spring.datasource.username=postgres
spring.datasource.password=secret
